{
  "name": "github-issue-survey-system",
  "version": "1.0.0",
  "description": "A survey system powered only by GitHub Issue Forms and GitHub Actions.",
  "type": "module",
  "scripts": {
    "generate:issue-templates": "node scripts/generate-issue-templates.js",
    "validate:surveys": "node scripts/validate-surveys.js",
    "start": "npm run generate:issue-templates && npm run validate:surveys"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}